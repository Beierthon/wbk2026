# Implementation Plans

Generated by the improve skill (deep audit) on 2026-07-08, against commit `2c0bb086`. Execute in the order below unless dependencies say otherwise. Each executor: read the plan fully before starting, honor its STOP conditions, and update your row when done.

Audit context: full-repo deep audit across correctness, security, performance, tests, tech debt, dependencies, DX, docs, and direction. The single biggest structural fact shaping these plans: commit `5ad320ca` made the Lager worker view the sole routed UI, leaving ~70% of `apps/web/components`, several lib engines, and multiple API routes unreachable while the docs still describe the removed dashboard. These plans default to the **Lager-only** product wedge; if the operator decides to restore the cockpit UI instead, plan 004 must not run and the direction items below become the roadmap.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| [001](./001-test-baseline-write-boundaries.md) | Test baseline for write boundaries (action, Supabase writes, import parsers, stock UI) | P1 | M | — | TODO |
| [002](./002-lager-write-path-fixes.md) | Lager stock write-path fixes (cache invalidation, demo fallback, lost updates, stale UI) | P1 | M | 001 | TODO |
| [003](./003-production-security-alignment.md) | Security/docs alignment + guard on cost-bearing endpoints | P1 | M | — | TODO |
| [005](./005-worker-performance.md) | Worker page performance (realtime-context query, layout waterfall, livekit-client bundle) | P2 | M | — | TODO |
| [004](./004-dead-code-purge.md) | Dead-code purge + docs truth pass (Lager-only surface) | P2 | L | 001, 002 (recommended) | TODO |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale)

## Dependency notes

- **002 requires 001**: the characterization tests written in 001 pin the current stock-flow behavior; 002 intentionally changes some of it (new action argument, conflict result, refresh call) and must update those assertions consciously rather than working untested.
- **004 after 001/002 (recommended)**: 004 prunes `project-actions.ts` down to the live lager action; 001's tests plus 002's changes settle what "live" means in that file first. 004 also assumes the Lager-only wedge — confirm with the operator before running.
- **003 and 005 are independent** of each other and of 001/002, but both touch `apps/web/app/api/livekit/token/route.ts` (003 adds a guard at the top; 005 replaces the mid-function existence check). Merge in either order; the later one reconciles a trivial overlap.
- **003 ↔ 004 interaction**: 003 guards the ERP import route; 004 keeps that route (direction candidate). If a future decision deletes the ERP routes, the guard goes with them.

## Findings considered and rejected

Recorded so they are not re-audited next run. Source categories: BUG/SEC/PERF/TEST/DEBT/DEP/DX/DOC findings from the 2026-07-08 deep audit.

- **Open RLS policies as such (SEC-01)**: documented, deliberate hackathon decision in `docs/betrieb/supabase-sicherheit.md` (real roles deferred to issues #18/#19). The *drift* around it (stale service-role claim, unwarned production guidance, unguarded cost-bearing endpoints) is addressed by plan 003; the RLS rework itself is the #18/#19 epic, not a plan here.
- **Bugs located exclusively in dead code** (terminplan action NaN validation BUG-13, terminplan hardcoded project id BUG-08, conflict-report multi-write BUG-07 for unrouted flows, `speicherePlanAbgleich` test gap TEST-08, ERP-route/action duplication DEBT-10, ERP CSV export double dashboard load PERF-07): superseded by plan 004's deletion of the dead callers; revisit any surface that gets revived.
- **Full auth/session system for server actions (SEC-03)**: belongs to the #18/#19 role epic; plan 003's guard covers the spend/abuse-critical endpoints in the interim.
- **Rate limiting (SEC-18)**: infrastructure-level; premature for a demo app with no production traffic profile. Reconsider with real deployment.
- **Security response headers / CSP (SEC-13), cookie `secure` flag (SEC-14), health-endpoint info exposure (SEC-15), example-file project refs (SEC-16)**: low leverage for the current demo posture; batch them into the eventual production-hardening pass with #18/#19.
- **Slimming the `ProjectRepository` contract / `project-overviews.ts` (DEBT-06)**: L-effort refactor whose payoff depends on the final product wedge; deferred until that decision is stable (noted in plan 004).
- **Splitting `packages/domain/src/commands/index.ts` (DEBT-15)**: cosmetic modularization; merge-conflict pain does not currently justify churning a well-tested file.
- **Mock demo data vs `supabase/seed.sql` divergence (DEBT-02)**: real but M-effort with MED risk to existing Supabase environments; do it together with the next deliberate seed change, adding a parity test then.
- **Realtime subscription table narrowing (PERF-05) and COCO-SSD detector code-splitting (PERF-16)**: marginal next to plan 005's three fixes; noted as deferred in plan 005.
- **Sequential upserts in `applyMutation` (PERF-09/BUG-06 transactionality)**: correct fix is a Postgres RPC/transaction; deferred with the DB-level concurrency work noted in plan 002's maintenance notes.
- **CI Turbo caching / job parallelization (DX-07, DX-08, PERF-17)**: repo is small; CI wall time is not the bottleneck today.
- **ESLint `only-warn` removal (DX-01)**: valid but surfacing the violation backlog deserves its own focused pass after 001–004 reduce the surface area (dead code is likely a large share of the warnings). Re-audit after plan 004 lands.
- **Pre-commit hooks, `.editorconfig`, `.nvmrc`, prettier scope, domain format scripts, double vitest cold start (DX-06/10/13/14/15/16)**: housekeeping batch; none blocks correctness. Bundle into one DX pass if desired.
- **`pnpm setup` failure suppression and Linux login detection (DX-02/03/04)**: real onboarding friction, but agents/CI don't use it and human onboarding is rare right now; fix opportunistically when the script next changes.
- **Env/docs drift (`WBK_OPENAI_MODEL` default, `WBK_VISION_MODE` matrix, LiveKit vars missing from `entwicklung.md`, undeclared `NEXT_PUBLIC_VISION_STREAM_DETECT_MS`, `turbo.json` `globalEnv` bloat — DOC-02/03/05, DX-05/11/12)**: folded partially into plans 003 (env declaration) and 004 (docs truth pass); the remainder is a small follow-up once vision defaults are decided.
- **Dependency housekeeping (`shadcn` CLI in dependencies DEP-01, turbo version skew DEP-03, `workspace:^` vs `workspace:*` DEP-04)**: one-line fixes, no runtime impact; batch with the DX pass.
- **`pnpm audit --prod`**: not executed during the audit (read-only session without node_modules); run once in CI or locally and triage — cheap, unowned by any plan.

## Direction options (operator decision, not scheduled)

Grounded product options from the audit, in likely-value order. Each would become a design/spike plan on request:

1. **Close the vision loop in Lager** — `bestaetigeVisionUpdate` domain command, `/api/vision/confirm`, and confirm UX all exist but are unreachable; the Lager camera stops at drawing detection boxes. Mostly wiring; this is the pitch's core differentiator.
2. **Surface ERP import/export in the live UI** — import/export APIs and parsers are built and kept by plan 004; no reachable UI triggers them.
3. **Read-only analytics/cockpit page** — analytics engines are deleted by plan 004 (restorable via git); a one-page consumer of `getAnalyticsUebersicht` would answer the product's "five central questions".
4. **Decide the wedge formally** — update `docs/produkt/*` and the issue epics to either Lager-first (plan 004's assumption) or a phased cockpit restoration.
